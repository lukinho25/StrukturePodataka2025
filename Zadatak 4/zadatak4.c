#include <stdio.h>

/*
Head
firstPoly
secondPoly

Head = {.coeff = 0, .pow = 0, .next = NULL}
firstPoly = Head;
secondPoly = Head;
q = &Head;

td poly * Position;
td struct poly {
    int coeff;
    int pow;
    Position next;
    }Poly;

otvoriti dat
-buffer
if(sscanf(buffer, "%d %d %n", &coeff, &pow, &n) n != 2) {
    






*/











int main() {


return 0;

}