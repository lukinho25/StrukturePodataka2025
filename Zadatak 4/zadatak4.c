#include <stdio.h>

/*
Head
firstPoly
secondPoly

Head = {.coeff = 0, .pow = 0, .next = NULL}
firstPoly = Head;
secondPoly = Head;
q = &Head;

td poly * Position;
td struct poly {
    int coeff;
    int pow;
    Position next;
    }Poly;

otvoriti dat
-buffer
if(sscanf(buffer, "%d %d %n", &coeff, &pow, &n) != 2) {
    
-newEl = Null;
p = &firstPoly;
if(p->next == Null)
    - alokacija
    newEl -> next = p -> next
    p -> next = newEl





*/











int main() {


return 0;

}